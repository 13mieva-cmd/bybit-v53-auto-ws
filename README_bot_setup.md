# Telegram bot is configured

Use this `.env` with the scanner:
- TELEGRAM_BOT_TOKEN set
- TELEGRAM_CHAT_ID set
- watchlist and thresholds set

## Start
```bash
cp output/.env .env
npm install
npm start
```
